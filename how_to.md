# Initial Set Up

```
  mkdir express-intro
  cd express-intro
  touch app.js
  npm init -y
  npm install --save express
  git init
  echo node_modules > .gitignore
  echo "# Express Intro" > README.md
  git add .
  git commit -m "Initial project setup"
```
